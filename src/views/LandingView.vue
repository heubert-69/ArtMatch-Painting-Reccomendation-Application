<template>
  <div class="page">
    <video class="bg-video" autoplay muted loop playsinline>
      <source src="@/assets/BG.mp4" type="video/mp4" />
    </video>

    <img src="@/assets/LOGO1.png" alt="ArtMatch Logo" class="logo" />

    <div class="container">
      <h1 class="brand">ArtMatch</h1>
      <p class="tagline">Discover. Connect. Collect.</p>

      <router-link to="/login">
        <button class="btn">LOGIN</button>
      </router-link>

      <router-link to="/register">
        <button class="btn">REGISTER</button>
      </router-link>
    </div>
  </div>
</template>

<script setup>
</script>

<style scoped>
* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

.page {
  min-height: 100vh;
  position: relative;
  overflow: hidden;
}

.bg-video {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  object-fit: cover;
  z-index: 0;
}

.logo,
.container {
  position: relative;
  z-index: 1;
}

.logo {
  position: absolute;
  top: 10px;
  left: 20px;
  width: 40px;
  height: 40px;
  object-fit: contain;
}

.container {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  min-height: 100vh;
  gap: 16px;
}

.brand {
  font-family: 'ClimateCrisis', sans-serif;
  font-size: 5rem;
  font-weight: 900;
  color: #FE5D26;
  letter-spacing: -1.5px;
  filter: drop-shadow(0px 12px 30px rgba(255, 255, 255, 0.6), 0.6);
}

.tagline {
  font-family: 'JosefinSans', sans-serif;
  font-size: 1.3rem;
  color: #1a1a2e;
  margin-top: -20px;
  margin-bottom: 40px;
  letter-spacing: 1px;
}

.btn {
  width: 280px;
  padding: 15px;
  background-color: #1a1a2e;
  color: #ffffff;
  border: none;
  border-radius: 50px;
  font-size: 0.85rem;
  font-weight: 700;
  letter-spacing: 2px;
  cursor: pointer;
  transition: background 0.2s;
}

.btn:hover {
  background-color: #FE5D26;
}

a {
  text-decoration: none;
}
</style>